from Manifest import Manifest
import config
import Processor

class RepoToolManifest(Manifest):

    def __init__(self, manifestfile, folder, level):
        #execute Google repo tool
        pass

        #Processor.processNode(repo, folder, level+1)